device_type: ssh
job_name: '{job_name}'
priority: medium
visibility: public

metadata:
  # please change these fields when modifying this job for your own tests.
  docs-source: https://github.com/zwx168238/ci-scripts/tree/dev/boot-app-scripts/templates/BOOT_PXE
  docs-filename: d05-arm64-kernel-ci-boot-pxe-template.yaml
  kernel_defconfig : {defconfig}
  kernel_version : {kernel_version}
  kernel_tree : {tree}
  kernel_endian : {endian}
  platform.fastboot : {fastboot}
  device_tree : {device_type}

timeouts:
  job:
    minutes: 120
  action:
    minutes: 20
  connection:
    minutes: 10
actions:
- deploy:
    to: ssh
    os: {distro}
- boot:
    method: ssh
    connection: ssh
    failure_retry: 2
    prompts:
      - 'root@ubuntu:/#'
      - 'root@centos ~'
- test:
    timeout:
      minutes: 120
    definitions:
{test_definitions}
